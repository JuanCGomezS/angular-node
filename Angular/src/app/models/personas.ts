export interface PeopleModel {
  per_id: number;
  per_ident: string;
  per_first_name: string;
  per_last_name: string;
  per_email: string;
  per_birth_date: string;
  per_address: string;
  updatedAt: string;
  createdAt: string;
}
